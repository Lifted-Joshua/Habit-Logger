# Habit-Logger
C# console app to log, view, update, and delete habit occurrences using a local SQLite database. Built with ADO.NET (no ORM) as part of the C# Academy curriculum. Auto-creates the database and table on first run.
